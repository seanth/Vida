description: >
  Graphics output: the ContextFree (.cfdg) files for the combined
  bottom-up + side view and the 3D .dxf files, kept on disk (-c) so their
  contents are pinned. The external cfdg and ffmpeg programs are not run.
species: [species/generic]
stages:
  - seed: 808
    args: [-n, graphics, -w, "20", -s, "12", -t, "6", -g, bs, 3d, -c, -a, a, -f, n]
